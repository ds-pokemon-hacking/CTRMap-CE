import zones.%ZONENUM%.Actors;
import zones.%ZONENUM%.SCRID;

public class %CLASSNAME% {
	public static int g_mode = -1;
	public static int g_ai_flag = 0;
	public static int g_interactive_flag = 0;

	public static void main(){
		switch (g_mode){
			default:
				break;
		}
	}
}